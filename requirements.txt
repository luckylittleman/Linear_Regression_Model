# ── Runtime Dependencies ─────────────────────────────────────────────────────
# These are the minimum packages required to run the FastAPI backend and
# the ML model in production.

fastapi>=0.135.0
gunicorn>=22.0.0
uvicorn>=0.41.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pandas>=3.0.0
scikit-learn>=1.8.0
joblib>=1.5.0
numpy>=2.4.0
python-dotenv>=1.0.0
python-multipart>=0.0.22
pydantic>=2.12.0
